Config = {}

Config.PanicbuttonCommand = "panicbutton" -- The command to trigger the panic button

Config.AllowedJobs = {                    -- The jobs that are allowed to use the panic button
    "police",
    "ambulance",
    "mechanic"
}
